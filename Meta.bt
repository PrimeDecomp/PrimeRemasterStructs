typedef struct(FourCC type, uint64 size)
{
  local uint64 start <format=hex, hidden=true> = FTell();
  switch (type)
  {
      case "TXTR":
          STextureMetaData meta;
          break;
      default:
          byte data[size];
          break;
  }
  FSeek(start + size);
} Meta <open=true>;
